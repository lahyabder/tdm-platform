# دليل لوحة التحكم الإدارية الأمنة والتشغيلية | Guide de la Console d'Administration

هذا الدليل يوضح الهيكل الأمني وكيفية الوصول إلى لوحة تحكم منصة **TDM** بعد ترقيتها إلى المعايير الأمنية والتقنية الكاملة للإنتاج الفعلي (**Production-Ready**).

---

## 🔑 1. نظام الحماية وتسجيل الدخول | Sécurité & Connexion
تم إلغاء أي محاكاة محلية أو جلسات غير آمنة تمامًا. يعتمد نظام الدخول الآن على:
- **الرابط**: `/[locale]/admin/login`
- **آلية التحقق**: يتم فحص المدخلات أولاً في قاعدة البيانات السحابية بجدول المشرفين (`admin_users`) بمقارنة هاش التشفير المالح الآمن (**PBKDF2**). في حال خلو قاعدة البيانات من سجلات إدارية، يرتد النظام بشكل آمن لفحص المتغيرات البيئية الخادمة المحددة في ملف `.env.local` (`ADMIN_USER` و `ADMIN_PASS`).
- **حماية الجلسة**: عند الدخول الناجح، يتم تشفير الجلسة وتوقيعها رقميًا بخوارزمية **HMAC-SHA256** باستخدام مكتبة *Web Crypto API* القياسية المتوافقة مع خوادم الـ Edge. الرمز الناتج يُخزن في كوكيز آمن ذي خصائص أمان مشددة (`HttpOnly`, `SameSite=Strict`, `Secure`).
- **فحص الـ Middleware**: يتم فك تشفير وفحص صحة التوقيع الرقمي وتاريخ انتهاء الجلسة خادمًا في كل طلب إداري تلقائيًا. في حال حدوث أي تلاعب بالتوكين، يتم إيقاف الطلب فورًا وتحويل المستخدم لصفحة تسجيل الدخول مع مسح الكوكيز الفاسد.

> [!WARNING]
> **ملاحظة أمنية بالغة الأهمية:** تم استئصال كلمات المرور الافتراضية الثابتة من الكود تمامًا. يُرجى مراجعة وتكوين المتغيرات البيئية `ADMIN_USER` و `ADMIN_PASS` في ملف `.env.local` ببيئة الإنتاج بكلمات مرور قوية ومخصصة (مثال: `ADMIN_PASS=tdm2026secure`).

---

## 📡 2. هندسة البيانات وتكامل قاعدة البيانات | Database Integration & RLS
المنصة الآن متصلة بقاعدة البيانات بشكل كامل ومستقر بنسبة 100%:
- **سياسة الصلاحيات الأقل (Least Privilege RLS):** تم تطبيق جدار ناري لقاعدة بيانات Supabase بتفعيل **Row Level Security (RLS)** لجميع الجداول. تقتصر الصلاحيات العامة والمفتوحة للعموم عبر المتصفح على **القراءة فقط (SELECT)** لمنع أي محاولة تعديل خارجية.
- **تأمين عمليات الكتابة (Server-Side APIs):** تتم جميع العمليات الحساسة (الإضافة، التعديل، الحذف) حصريًا من خلال واجهات برمجية خلفية محمية خادمًا (Next.js Secure API Routes) مثل:
  - `/api/admin/news` (إدارة الأخبار)
  - `/api/admin/facilities` (إدارة المنشآت)
  - `/api/admin/licenses` (إدارة التراخيص)
  - `/api/admin/legislations` (إدارة التشريعات)
  - `/api/admin/downloads` (إدارة التنزيلات والملفات)
  - `/api/admin/users` (إدارة المستخدمين والأدوار)
- **مفتاح دور الخدمة (Service Role Key):** يتم تنفيذ العمليات من الخلفية الآمنة بعد إثبات سلطة الإداري، وذلك باستخدام مفتاح `SUPABASE_SERVICE_ROLE_KEY` المشفر والمحمي في الخادم لتخطي RLS بأمان تام.

---

## 💻 3. المميزات المتوفرة في لوحة التحكم | Fonctionnalités de la Console

1. **📊 لوحة قياس ديناميكية (Dashboard):** عرض حي للإحصائيات والتراخيص المنتهية والمنشآت النشطة متصلة مباشرة بقاعدة البيانات.
2. **📰 إدارة الأخبار (News Management):** إضافة وتحرير الأخبار وتحديثها بشكل فوري مع التحقق من تماسك واجهة المستخدم وسلامة السحابة.
3. **🏢 المنشآت الإعلامية (Media Facilities):** التحكم بالبث الرقمي ونسب الإشراك، مع إمكانية التصدير بصيغة **CSV**.
4. **📜 التراخيص والتشريعات (Licenses & Legal):** إدارة التراخيص وتواريخ انتهائها وإصدار النصوص والقرارات والتعاميم المعتمدة وربطها بالمنشآت.
5. **👥 إدارة المستخدمين والأدوار (User Management - RBAC):** شاشة كاملة لإدارة حسابات المشرفين الإضافيين وتعيين أدوارهم (`admin`, `editor`, `viewer`) وتعديل حالاتهم بشكل حقيقي في جدول قاعدة البيانات الآمن.

---

## 🛠 4. التثبيت والتشغيل في بيئة التطوير والإنتاج

```bash
# 1. تثبيت الاعتماديات النظيف والمتماسك
npm ci

# 2. تشغيل بيئة التطوير المحلية
npm run dev

# 3. بناء بيئة الإنتاج للتحقق من جودة الكود والأداء
npm run build
```

---
*تم التطوير والتقوية الأمنية لشركة البث الإذاعي والتلفزي الموريتاني (TDM) - 2026*
